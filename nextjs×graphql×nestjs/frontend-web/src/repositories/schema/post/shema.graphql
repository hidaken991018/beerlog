query Posts {
posts{
id
  title
  text
  isPublished
}
}

mutation CreatePost($title: String!, $text: String!) {
  createPost(input:{
    title:$title
    text:$text
  }){
    id
    title
    text
    isPublished
  }
}
